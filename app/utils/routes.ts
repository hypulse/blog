type Route = {
  name: string;
  path: string;
};

const routes: Route[] = [
  {
    name: "Blog",
    path: "/blog",
  },
  {
    name: "Search",
    path: "/search",
  },
];

export default routes;
