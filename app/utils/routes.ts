/**
 * Array of routes for the Navbar menu
 */
const routes: { name: string; path: string }[] = [
  {
    name: "Blog",
    path: "/blog",
  },
  {
    name: "Search",
    path: "/search",
  },
];

export default routes;
